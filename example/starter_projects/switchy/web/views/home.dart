part of switchy;

/**
 * Represents the home content view of Switchy.  It's a simple view, so we
 * won't need a dedicated view model for it.
 */
class Home extends View
{
    Home() : super.fromResource('web/views/templates/home.xml');
}
